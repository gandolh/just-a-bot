SSH_HOST=<ssh-alias>

# OR:
# SSH_USER=<user>
# SSH_HOSTNAME=<host-or-ip>
# SSH_IDENTITY_FILE=~/.ssh/<key>
# SSH_PORT=22

# Where the repo lives on the server.
REMOTE_DIR=~/<repo-dir>

# Node major version the pre-deploy phase installs (NodeSource).
NODE_MAJOR=22

ENV_FILES=<ENV_FILES>
